# Synchronization

For redundancy and to provide faster service, we aim to have multiple AI-on-Demand nodes across Europe.
These main nodes are referred to as "root nodes".
Realization of the synchronization mechanism is work in progress.